{
    "name": "openpnp-types",
    "private": true,
    "version": "0.1.0",
    "description": "TypeScript types for OpenPnP Nashorn scripts",
    "types": "types/index.d.ts",
    "files": [
        "types"
    ],
    "scripts": {
        "generate-types": "./generate-openpnp-dts.sh",
        "typecheck:examples": "tsc -p tsconfig.json --noEmit",
        "typecheck:examples:watch": "tsc -p tsconfig.json --noEmit --watch",
        "build:examples": "tsc -p tsconfig.json",
        "watch:examples": "tsc -p tsconfig.json --watch",
        "build": "pnpm run build:examples",
        "clean": "rm -rf build"
    },
    "devDependencies": {
        "typescript": "^5.9.2"
    },
    "license": "MIT"
}